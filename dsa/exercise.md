# 准备面试华为 od 岗刷的题

# 02.25

## 200 分 最小外接凸多边形 思路待更新

用时约 3h

## (A 卷,100 分)- 处理器问题（Java & JS & Python）

用时约 30min

# 02.26

## (100 分)  压缩日志查询（Java & JS & Python&C/C++）

理解题意用时 9min
设计解决方案 2min
编程 25min
测试 8min
总计 43min

1. 整理成数组
2. 取出 startIndex,endIndex.
3. 在此区间内循环。
4. 每个元素是一个区间。分别在开头、中间、结尾处理为要输出的数据，放入 res 数组。
5. 输出

2 层循环

## (100 分) - 补种未成活胡杨（Java & Python& JS & C++ & C ） 不会

理解题意用时 5.5min
设计解决方案 8min
编程 1h
测试  
学习 2h
总计

1. 用双指针确定滑动窗口。
2. 右指针从 0 滑到最后，每期间窗口内死树大于 k 时左指针右滑，选择出最大的空间值。

滑动窗口

- 双指针
- start + len

## 100 小朋友排队

学习 13min

1. 要求高低相间、移动最少，所以只考虑相邻 2 个小朋友。
2. 用滑动窗口取出 2 个小朋友。再用 flag 标记先高还是先低。

滑动窗口

## (100 分) - 构成正方形的数量（Java & Python& JS & C++ & C ）

学习 40min
不会

## (100 分) - 恢复数字序列（Java & Python& JS & C++ & C ）

学习 11min

尝试 n 个连续的数字的统计。用暴力破解的。

# 02.27

## (B 卷,100 分)- 计算最接近的数（Java & JS & Python & C）

学习 约 1.5h
考滑动窗口
练习题的答案好像错了。

1. 排序
2. 算出中位数
3. 按长度 k 滑动窗口，求窗口内的和，再求绝对值。
4. 迭代求最小差。

滑动窗口

## (100 分) - 开心消消乐（Java & Python& JS & C++ & C ）

理解题意用时 8.5min
设计解决方案 6min
编程 53min
学习 9min
练习题的答案好像错了。
我觉得应该使用回溯解题。

1. 找到所有需要翻的 1,组成 choices.
2. 若 choices.length，则返回用回溯处理；否则使用 state 放入 res
3. 按要求返回

回溯+矩阵
let res = []
let f = (arr, state) => {
let choices = arr.filter(有 1)
let res = []
if (choices.length) {
for (let point of choices) {
state.push(point)
let tArr = matrixOp(point)
tArr.forEach(point = 0)
f(arr, state)
tArr.forEach(point = 1)
state.pop()
}
} else {
res.push([...state])
}
按要求输出
}

## (100 分) - 考勤信息（Java & Python& JS & C++ & C ）

理解题意用时 4min
设计解决方案 18min
编程  
学习

1. 用滑动窗口处理窗口内的数据。

## (100 分) - 螺旋数字矩阵（Java & Python& JS & C++ & C ）

理解题意用时 + 设计解决方案 12min
题目错了。

1. 求出列数
2. 用 4 个 while 转圈填充++i 或\*

## (100 分) - 生成哈夫曼树（C++ Java JavaScript Python C 语言）

学习 hufmantree 17min
设计解决方案 24min

1. 把数据处理为对象，再放入数组。
2. 排序数组后取出 2 个最小值，组成 1 个新节点，再放入数组。直到数组长度为 1
3. tree = arr[0]
4. 中序输出。

## (100 分) - 手机 App 防沉迷系统（Java & Python& JS & C++ & C ）

理解题意用时 5.5min
设计解决方案 21min
学习 18min

arr 注册表 [star, end, app]...

```
1. 找出有冲突的旧块
比较优先级         注册
删旧添新   null
1. 查询时刻
```

我用离散化思路解决的。
作者用“是否重叠”解决的。作者的更简单。
从注册表右端开始判断若重叠，则删除。`if (Math.max(block1Start, block2Start) < Math.min(block1End, block2End)) { 删除 }`
从数据右端开始删除元素不会影响 splice 的参数。

1. 创建注册表。
2. 每一个 app+时间 处理为一个对象
3. 整理出所有与新块冲突的旧块。
4. 取出这些旧块的最高优先级，再与新块的优先级比较。
5. 删除旧块，添加新块。

## (100 分) - 运维日志排序（Java & Python& JS & C++ & C ）(1)

理解题意用时 4min
设计解决方案 1min

时间可以转换为

- ms
- 每段数值\*相应进制，再求和。

1. 处理为数组。
2. 用 sort 排序 str
3. 返回排序结果。

## (100 分) - 字符串分割转换（Java & Python& JS & C++ & C ）

理解题意用时 5.5min
设计解决方案 14min
学习 2min

1. 按-分割 str 为 arr
2. 取出第一个，
3. 取出第二个及以后。
4. 按长度 k 分割。
5. 统计每块的大小写数量，
6. 转换大小写
7. 再拼接为 str.

## (100 分) -虚拟理财游戏（Java & Python& JS & C++ & C ）

理解题意用时 7min
学习 16min

1. 循环求风险值内的最大收益，
2. 再循环求 2 个项目的风险值内的最大收益。

暴力破解

## (100 分)- 比赛评分（Java & JS & Python）

理解题意用时 6min
学习 20min

1. 洗数据
2. 每个选手的数据处理为一个对象，再组成数组。
3. 选手的分数按倒序排列。
4. 排序该数组，先按总分排序，再按高分大的排序。

2 层循环+排序

## (100 分)- 查找单入口空闲区域（Java & JS & Python）

理解题意用时 2.5min
设计解决方案 30min
编程 约 2h

1. 找到所有入口，组成数组 entryArr
2. 遍历 entryArr,对每个 entry 使用 bfs 处理，返回坐标，再放入 spaceArr.
3. 统计 spaceArr 中的多少个 entryArr 中的数据。若大于 2 个，则舍弃；否则，放入 res.
4. 按 res.length 格式化输出。

矩阵+bfs

# 02.28

## (100 分)- 乘坐保密电梯（Java & JS & Python）

理解题意用时 5.5min
设计解决方案 26min
编程 40min
学习+修改代码 46min

解题逻辑

1. 全排列所有可能
2. 定义计算最后到达楼层的方法
3. 遍历，分为可以到达目标楼层的数组和小于目标楼层的数组。
4. 判断是否存在可以到达目标楼层
5. 排序不到达的数组，取出第一个，再输出。

回溯

## (100 分)- 代码编辑器（Java & JS & Python）

理解题意用时 4.5min
设计解决方案 2min
编程 28min
学习+修改代码 7.5min

1. switch 分类
2. 处理每个命令

操作字符串

## (100 分)- 端口合并（Java & JS & Python）

理解题意用时 7.5min
设计解决方案+编程 37min
学习 3min

1. while 检查是否存在需要合并的，再合并，直到没有。
2. 检查：双层循环、从右到左，是否存在交集长度>=2 的.返回 i,j
3. 合并：先赋值小的，再删除大的。

矩阵

## (100 分)- 对称美学（Java & JS & Python）

理解题意用时 7.5min
设计解决方案+编程 37min
学习 约 3h

不要信题里说的规律。自己找规律。
数值大于 2^53 - 1 时使用 BigInt。
注意精度

1. 计算出 2 ^ n 数组。
2. 定义前几个字符串。
3. 当行数大于定义的几个字符串数量时，利用第 i 行的文本=第 i-1 行取反+第 i+1 行的规律求解。
4. 返回结果。
5. 当 n>已定义字符串数量时，判断 2^(n-1) <= k 2^(n-2) <=k 分别处理 n,k

先把输入处理为程序可以直接使用的数据。
找规律+精度

## (100 分)- 符合要求的元组的个数（Java & JS & Python & C）

1. 用回溯求出所有情况。
2. 再去重

回溯+去重

## (100 分)- 机器人（Java & JS & Python）

1. 2 层循环+bfs.
2. bfs 中把 state 放入 res 数组
3. 循环时若当前点不在 res 中，则执行 bfs。

bfs.

## (100 分)- 矩阵扩散（Java & JS & Python & C）

1. queue/bfs 同级
2. while 判断是否全为 1.循环 1 次 bfs(),time++.
3. bfs 内处理当前 queue 内的元素，并向 queue 放入相邻元素。

bfs

## (100 分)- 路灯照明问题（Java & JS & Python & C）

1. 循环从 0 到 len-1 的元素，累加该 i/i-1 的间隙。
2. 返回 res

循环或滑动窗口

## (100 分)- 模拟消息队列（Java & JS & Python）

1. 处理好 nArr,mArr.
2. 定义 res 矩阵，长度为 m.
3. 排序 nArr,(不排序 mArr)
4. 循环 nArr.在 mArr 中倒序找到第一个符合`item[0]<=t<item[1]`，取出对应下标 index.
5. 在 res[index].push(xxx)
6. 输出

操作数组

## (100 分)- 提取字符串中的最长合法简单数学表达式（Java & JS & Python & C）

1. 用正则取出表达式
2. eval()

正则

# 03.01

- 2 个 for 循环
- 3 排 2 后遍历

## (100 分)- 跳房子 I（Java & JS & Python）

2 层遍历

## (100 分)- 完美走位（Java & JS & Python）

1. 算出平均值
2. 算出大于平均值的字母及数量，再求出它们的数量和。（表示需要该字母多少个。）
3. 定义 2 个指针。都从 0 开始。
4. 向右移动 right 指针时，若指向统计中的字母的数量大于 0，则数量和--。在统计中减少该字母的数量。
5. 当数量和为 0 时，右移 left 指针。若 left 指向统计中的字母>=0，则数量和++。在统计中增加该字母的数量。期间更新最短长度。

滑动窗口

## (100 分)- 网上商城优惠活动（Java & JS & Python）

1. 定义打折的方法。返回打折后价格、使用的卷数量。
2. 2 层遍历打折组合方法。

2 层遍历

## (100 分)- 相对开音节（Java & JS & Python）

1. 反转，分割。
2. 定义正则，找到匹配文本。

正则

## (100 分)- 异常的打卡记录（Java & JS & Python）

1. str => arr
2. 定义检查当前值是否异常的方法。
3. 定义检查 2 个值是否异常的方法。
4. 从 0 到 len-1，分别检查当前值和当前值与后一个值是否异常。若存在异常，则在 set 对象中放入下标。
5. 根据下标从 arr 中取出对应数据。

滑动窗口+去重

## (100 分)- 整理扑克牌（Java & JS & Python）

1. 定义一个 len = 5 的数组。
2. 降序排列 arr.
3. 定义 push 方法。每次取出前 4 个，封装成对象，分别放入下标是 0、2、3、4 的 group 中。直到取完。
4. 用滑动窗口把数值相同的牌喂给 push 方法。
5. 把 2 从左向右排，把 3 从右向左排组成葫芦后放入 1.
6. 若 2.len>1，则最前面和最后面组成葫芦后放入 1.单牌放入 4.
7. 排序 4.
8. 输出。

排序

## (100 分)- 整数编码（Java & JS & Python）

1. n -》 bigint
2. toString(2) 再转换为 2 进制 string
3. 从右向左取 7 位，前面+1,最后一次取时前面加 0.
4. 依次追加到数组。

parseInt(s, 2) // 把 s 当作用 2 进制解析。
number.toString(16) // 数字转换为 16 进制形式的字符串

## (100 分)- 字符串摘要（Java & JS & Python & C）

1. `[0,len)`遍历。若连续，则计算连续的数量，否则计算其后面相同字母的数量。再放入数组。
2. 数组排序。

## (100 分)- 最小调整顺序次数、特异性双端队列（Java & JS & Python）

1. str => arr
2. 定义 isOrder 方法，判断是否有序。
3. 定义排序方法
4. 遍历 arr.处理命令。当需要输出时判断是否有序。若无序，则排序，count++.
5. 返回 count.

# 03.02

## (100 分)- 最长的密码（Java & JS & Python）

1. str -> arr 按长度倒序排列
2. 遍历每个元素，元素依次从尾减到 0.是否都在数组中。返回第一个。

2 层循环

## (100 分)- 最长的顺子（Java & JS & Python）

1. str -> arr.
2. 列出全部的牌放在对象中。
3. 统计手中的牌+出过的牌。在全部牌中减去它们。
4. 在剩下的牌中取出牌数为 0 分割的数组。
5. 求出全部顺子的。按长度倒序、数值最大排序。

统计+排序

## (100 分)-猜数字（Java & Python& JS & C++ & C ） 不会

## (100 分)-斗地主之顺子（Java & Python& JS & C++ & C ）

1. str=》arr 去掉不能组成顺子的 -》 升序排序。
2. 定义 2 个指针。定义滑动窗口，若连续，则右移右指针。
3. 遍历数组。取出连续区间。放入数组。去掉长度小于 5 的。
4. 输出。

滑动窗口

## (100 分)-流浪地球（Java & Python& JS & C++ & C ）

1. str => arr
2. 排序 arr
3. 定义取邻点的方法
4. 定义队列
5. `while queue.length`
   1. 按时间入队列。
   2. 当前要启动的 // arr
   3. 启动 arr
   4. 邻点入队列
   5. time++

数组循环+bfs

## (100 分)-敏感字段加密（Java & Python& JS & C++ & C ） 不会

## (100 分)-增强的 strstr（Java & Python& JS & C++ & C ）

1. str => reg
2. reg.exec(str)

正则

## (100 分)-最大利润贪心的商人（Java & Python& JS & C++ & C ）

1. str => metrix
2. 每种商品的相邻 2 天的价格差\*数量再累加。

2 层循环+贪心

## (100 分)-最左侧冗余覆盖子串（Java & Python& JS & C++ & C ）

1. 统计 s1 中数据
2. 从 0 开始创建一个长度为 k 的滑动窗口。统计窗口内的数据。及需求总量。
3. 2 者相减，得到还需要什么字母多少个的统计表。
4. 整体移到窗口，若需要的字母进窗口，则统计表--，若需要的字线出窗口，则统计表++。若需求总量为 0，则返回左指针。当右指针大于 s2 长度是返回-1.

滑动窗口

## (200 分) - 特殊的加密算法（Java & Python& JS & C++ & C ） 不明白

## (200 分) - 智能驾驶（Java & Python& JS & C++ & C ）

1. 按初始油量 100 算，用回溯法找到达终点的所有路径。若路径数量为 0,返回-1，否则，找到其中到加油站或终点用油量最小的。
2. 返回最小用油量。

回溯时 tag 用二态。因为用于搜索所有可能。
bfs 时 tag 用三态或二态。
返回一圈一圈的值时用二态
返回连通区间时用三态

## (200 分) -文本统计分析（Java & Python& JS & C++ & C ） 不明白

## (200 分) -周末爬山（Java & Python& JS & C++ & C ）

1. str => arr
2. 取出最高点组成的数组 highDescList。arr-压平-倒序-高度去重
3. 定义 getShortestPath(i, j).返回到该点的的最短距离。
   1. 定义等 map 的 tag 数组。
   2. 定义最短距离的 metrix.distMetrix
   3. 起点入队列。遍历队列，直到空。取出该点的邻点，再在 distMetrix 中设置值。
   4. 到`[i,j]`时返回最短距离。
4. 遍历 highDescList。取到该点的最短距离。
5. 判断最短距离是否为 0.若不为，则返回高度和距离，否则返回 0 0

最短距离

# 03.03

## (200 分)- 出错的或电路（Java & JS & Python & C）

1. str => arr
2. i=0 i < len - 1, 把 i 和 i+1 交换。再与 bArr 求或运算。再判断是否相同

## (200 分)- 分积木（Java & JS & Python & C） 不会

## (200 分)- 高效的任务规划（Java & JS & Python & C）

1. str => arr `[手动，机器]`
2. 任务按机器时长降序排列
3. 累加手动时长+最后一个机器时长。

## (200 分)- 根据 IP 查找城市（Java & JS & Python & C）

1. str => arr .map(str => number) // 按进制转换
2. target => number
3. for arr。 找到在区间,且空间范围最小。
4. 返回值

洗数据

## (200 分)- 计算误码率（Java & JS & Python & C）

1. 解码 2 个压缩文本
2. 统计同位不同字母的数量。
3. n/len

## (200 分)- 开放日活动、取出尽量少的球（Java & JS & Python）

1. 求出 2 端边界，
2. 再求出理想边界时的结果。
3. while 边界不能重合。
4. 求二分位时的结果 t.
5. 若和大于 sum,则重置上边界，若和小于 sum,则重置下边界+重置 res.若相同则重置 res+break。
6. 返回 res

二分法。
每次迭代时都要缓存当时结果。

## (200 分)- 快递投放问题（Java & JS & Python）

1. 猜题意。（可以从题目描述、用例）
2. 要投递的 str => {packageN: [A, B]}[] 1
3. 不能投递的 str => {packageN: [A, B]}[] 2
4. for (1 中的包裹) 若这个包裹的起点&终点在 2 中，则 res.push(packageN)

洗数据

## (200 分)- 篮球游戏（Java & JS & Python & C）

1. 循环要出的数组。
2. 若与首相等，则 res.push(L).若与尾相等，则 res.push(R).否则从入数组中添加一个到桶中。
3. 当`i<=inputLen&&j<outputLen`成立时，判断桶中是否球。
4. 输出

按描述编写代码

## (200 分)- 区间交集（Java & JS & Python）

先选择交集，再求交集列表的并集。

1. 按区间起点升序排列
2. 2 层循环求出交集列表
3. 若交集列表的长度为 0，则返回 none,否则取并集。
4. 交集列表以开头升序排序。
5. 以第一个元素为范围，开始循环从 i=1 开始，若 i 的起点小于等于范围的终点，则合并到范围中。否则 res.push(range).重置 range.
6. res.push(range)
7. 输出

交集+并集

## (200 分)- 贪心歌手（Java & JS & Python & C） 不会

## (200 分)- 田忌赛马（Java & JS & Python & C）

1. 回溯取出大于这些位置上的数字，并且不能重复。
2. 输出 res.len

回溯

## (200 分)- 寻找最大价值的矿堆（Java & JS & Python）

1. 定义等大 tag
2. 定义 bfs.返回该点的连通区域。
3. 2 层循环。若 tag 中未处理此点，则 bfs(i, j, tag)的返回值放入 res。
4. res 内求和,再排序，再返回`[0]`

bfs.

## (200 分)- 寻找最优的路测线路（Java & JS & Python & C） 不会

1. 定义 path.
2. 定义 bfs()，返回该点的周边 4 点。
3. path.push(start)
4. while(isEnd)
5. bfs(path.shift()),再取出最优的点，再 push

## (200 分)- 优雅子数组（Java & JS & Python）

1. i = 0; j = 0
2. while i < len
3. 修改当前字母的统计量 obj[c]++
4. 若统计量大于等于 k，则其后的子数组都满足条件，所以都加起来。统计量--（减去本次的统计量）
5. 否则 j++

滑动窗口的计算次数少于 2 层循环（即暴力）
滑动窗口
移动指针是注意修改统计量

200 分的题要认真对待。它不是简单的。

# 03.04

考试

# 03.05

## (200 分)- 员工派遣（Java & JS & Python & C） 不会

## (200 分)- 字符串解密（Java & JS & Python） 不会

## (200 分)- 字符串拼接（Java & JS & Python & C）

1. str -> arr
2. 回溯。剪枝当前字母不能与最后一个相邻。

回溯

## (200 分)- 组装新的数组（Java & JS & Python）

1. 取出数组 N 的最小值-1 min
2. 回溯。当数组和小于 m-min 时回溯，当数组和大于等于 m-min 且小于等于 m 时放入 res.否则停止回溯。
3. 输出

回溯

## (200 分)- 组装最大可靠性设备（Java & JS & Python）

1. str => arr =》 metrix
2. 回溯取出所有可能。
3. 算出所有可能的最小可靠性。
4. 排序。
5. 返回第一个。

回溯

## (200 分)- 最佳植树距离（Java & JS & Python & C）

1. str > arr
2. 求出最佳间隔
3. 求出可种树的下标组成数组。
4. 从最佳间隔到 0 遍历 step。定义 2 个指针 i j =i+step。若 j 不能种树时 j++,否则 i = j j++ 直到`j<len`不成立
5. 当树的数量大于等于树苗的数量时停止循环。
6. 输出 step

处理边界数据

## (200 分)- Wonderland（Java & JS & Python & C）

1. 定义 dp
2. 每个值用 4 个值的最小值填写。
3. 取出相应数据。

no.i 天的费用可以是 no.i-1 + 1 日票也可以是 no.i-3+3 日票。。。
在 no.i-3+1 日卖 3 日票。

动态规划

## (200 分)-字母组合过滤组合字符串（Java & Python& JS & C++ & C ）

1. 定义 map
2. str => arr 再映射得到字符串组成的数组。此时得到 metrix
3. 回溯 metrix。剪枝含全部屏蔽字符的组合。
4. 输出

回溯

## 100 分 采购订单

1. str => metrix
2. 分为 gui/jian2 组
3. jian 组，从右向左合并，再打折，再排序，再输出。
4. gui 组、排序、输出。

编程能力

## 100 分 分弹珠游戏

1. 分时记录次数
2. 输出

编程能力

## 100 分 国际移动用户识别码(IMSI)匹配

1. 取出\*、？分别处理为对应字符串。再拼接。得到了该配制可以对应的所有字符串。
2. 判断目标字符串中是否在这些字符串中。
3. 输出

回溯+暴力

# 03.06

## 100 分 商品推荐多属性排序

1. metrix 排序

排序

## 100 分-敌情监控

1. 定义一个数组。
2. 按命令操作数据。
3. 当 query 时，滑动窗口查询最小数量，输出结果。

滑动窗口

## 100 分-面试叫号系统 不理解题意

## 100 分-停车场收入统计

1. str => map
2. 循环 map.values().不处理包月车辆。
3. 计算出时长区间与不计时区间的交集，再减去。再算出费用，

区间交集+编程能力

## 100 分-小明减肥

1. 用回溯方法求出所有情况。

回溯+数组去重。
因数组内含 0 所以不能使用求积去重。需要使用排序后字符串去重。

# 03.07

## 100 分-资金投资

1. 去掉超过风险的。
2. 用回溯求出所有组合。
3. 倒序排列，取出第一个。

排序+回溯

## 200 分-项目排期最少交付时间

1. 按工作量倒序排列 arr
2. 创建长度为 n 的 metrix。
3. 从头遍历 arr,总是给工作量小的追加工作。
4. 把工作量和倒序排列。
5. 取出第一个的工作量和

排序

## 100 分，整理版本号

1. str => arr。 这里比较复杂。
   1. 小写转换为大写。
   2. 统计 VRCB 出现次数。若不符合题意，则为脏数据，舍弃。
   3. 按 VRCB 的下标分段取字符串。再使数字达到 3 个。再按 VRCB 排接。
2. 去重。
3. 取第一个。或返回-1.

排序+编程能力

## 200 分 产品模块算法检验

1. 整理出所有模块的数组。
2. 再整理出所有模块的信赖关系，再放入所有模块的数组中。
3. 遍历模块的数组，记录并依次找到 prev，直到出现记录中出现，则去掉开始重复的前面的内容，再放入 res 中。
4. 去重 res.
5. 返回 res.length;

链+去重

# 03.08

## 200 分 统计差异值大于相似值二元组个数

1. 定义差异值方法。
2. 定义相似值方法。
3. 2 层循环。若差异值大于相似值则 count++

二进制

## 200 分 找到它

1. 2 层循环。依次判断是否等于目标值。若等于则保存【i,j】并且 break;
2. 从当前 i，j 是否可以找到匹配目标值的字符串。
   1. 用回溯找到当前值是否有匹配当前值的。
3. 输出 i,j

回溯

## 200 分 最小外接凸多边形

又做了一次

1. 定义求向量叉积的方法。
2. 2 层循环。每次全循环。判断向量叉积是否全大于 0，记入 set.
3. set 转换为坐标的数组。按横坐标升序排序。循环求纵坐标小于等于横坐标的点，再循环求纵坐标大于横坐标的点。

向量+循环+排序+数学函数

## 200 分- 故障树诊断 看不懂

## 200 分-水库溃坝填补

1. 用回溯得到所有填补可能。
2. 记得所有可能的缺口量、多余量、使用量。
3. 排序，先按缺口量降序排序，再按使用量升序排序。
4. 返回最一个值。

排序+回溯

## 200 分-项目排期最少交付时间

1. 工作量按降序排序。
2. 定义一个对象，可以容纳 n 个元素。
3. 定义一个取最小工作量的对象的方法。
4. 循环工作量数组。每次为最小工作量的对象添加工作量。

排序

## 新版游戏分组

同上题

## 字符串计数匹配

1. 定义 2 个指针。都从 0 开始。
2. 当 j 右移时统计。
   1. 当字母量大于等于 k 且数字全出现时，右移 i,修改统计。

统计

# 03.09

## 200 分-模拟数据序列化传输 不全会

会加密
不会解密

1. 定义类型与类型的码值的映射。
2. 定义符号的栈、密文对象的栈。
3. 定义取出
4. 从 0 开始，直到 str.length.若遇到`[`则 signArr.push(xx),若遇到`]`,则取出最近`[`与`]`之间的密文对象，再 signArr.pop()
5. 当 signArr.length 为空时 res += strArr.pop().mt
6. 输出。

栈

## 需要复习的知识点

向量
回溯+剪枝
dp
正则
最长连续子串
位运算
做 od 题时的沉淀笔记。
2 点的最短距离
number/string/math/bigInt 等基本类型的 api

## 沉淀

- [矩阵 metrix](/dsa/metrix.html)
- [array](/dsa/array.html)
- [树 tree](/dsa/tree.html)
- [滑动窗口 slideWindow](/dsa/slideWindow.html)
- [统计 statistics](/dsa/statistics.html)
- [并查集 unionFind](/dsa/unionFind.html)
- [矩阵 metrix](/dsa/metrix.html)
-

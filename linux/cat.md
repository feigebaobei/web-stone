# cat
读取指定文件

```shell
cat filename

cat file1 file2 # 读取多个文件
cat -n filename # 读取文件后并显示行号
cat > newFile   # 创建一个文件，并在命令行中录入内容。
cat -s file     # 把多个空行压缩后输出。
cat file1 >> file2  # 把file1追加到file2
cat file1 file2 > file3 # 把file1/file2合并到file3
cat >> file     # 在已经存在的文件中追加内容。



```